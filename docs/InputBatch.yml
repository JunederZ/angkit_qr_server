Add new batch to the database.
---
tags:
  - Batch
parameters:
  - name: Body
    type: json
    in: body
    required: true
    schema:
      $ref: '#/definitions/templateInputBatch'
definitions:
  templateInputBatch:
    type: string
    properties:
      id:
        type: string
        example: Bebek
      jenisTernak:
        type: string
        example: Bebek
      peternak:
        type: string
        example: FOREIGN KEY PETERNAKAN
      distributor:
        type: string
        example: FOREIGN KEY DISTRIBUTOR
      nama:
        type: string
        example: Batch 1 - kandang A
      spesies:
        type: nama
        example: Bebek Kampung
      beratRata:
        type: integer
        example: 9.2
      tanggalMulai:
        type: string
        example: DATE IN ISO FORMAT
      tanggalPotong:
        type: string
        example: DATE IN ISO FORMAT
      tanggalKemas:
        type: string
        example: DATE IN ISO FORMAT

responses:
  201:
    description: Success add new batch to database
  400:
    description: missing field